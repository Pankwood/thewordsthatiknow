name: End-to-end tests 🧪

on: deployment

jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/vercel-cypress@v1
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          repo: NaiaOzorio/the-words-that-i-know-end-to-end
          token: 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
