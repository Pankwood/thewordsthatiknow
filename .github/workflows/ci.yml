name: End-to-end tests 🧪

on:
  push:
    branches: [ "stage" ]
  pull_request:
    branches: [ "stage" ]

jobs:
  regression:
    runs-on: ubuntu-latest
    steps:
        uses: actions/checkout@v3
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          repository: NaiaOzorio/the-words-that-i-know-end-to-end
          token: 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

          uses: bahmutov/npm-install@v1

          uses: cypress-io/github-action@v4
